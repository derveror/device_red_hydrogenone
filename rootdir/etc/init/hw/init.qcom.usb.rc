# Standard Android configfs handles the common compositions. RED kernel exposes
# the MSM8998 DWC3 controller at a800000.dwc3.
on boot
    setprop sys.usb.controller a800000.dwc3
    setprop sys.usb.configfs 1
